# Django Settings
DJANGO_SECRET_KEY=your-secret-key-here-change-in-production
DEBUG=False
ALLOWED_HOSTS=localhost,127.0.0.1,your-app-name.onrender.com

# Database (SQLite for MVP, PostgreSQL for production)
DATABASE_URL=sqlite:///db.sqlite3
# DATABASE_URL=postgresql://user:password@host:5432/dbname

# Telegram Bot
TELEGRAM_BOT_TOKEN=your-telegram-bot-token-here
TELEGRAM_BOT_USERNAME=your_bot_username_without_at
TELEGRAM_WEBHOOK_SECRET=your-webhook-secret-here

# Manual API protection
API_AUTH_TOKEN=your-manual-api-token-here

# Django Superuser (for deployment)
DJANGO_SUPERUSER_USERNAME=admin
DJANGO_SUPERUSER_EMAIL=admin@example.com
DJANGO_SUPERUSER_PASSWORD=changeme

# Google Sheets
GOOGLE_SHEET_ID=your-google-sheet-id-here
GOOGLE_SERVICE_ACCOUNT_FILE=/etc/secrets/credentials.json
GOOGLE_SHEET_TAB_NAME=Complaints Register

# Order Approval Web App and media storage
APP_BASE_URL=https://your-app-name.onrender.com
MEDIA_STORAGE_PROVIDER=google_drive
MEDIA_MAX_FILE_SIZE_MB=20
ORDER_APPROVAL_MAX_FILES_PER_SLOT=10
ORDER_APPROVAL_MAX_TOTAL_UPLOAD_MB=60
GOOGLE_DRIVE_MEDIA_FOLDER_ID=your-shared-drive-folder-id
ORDER_APPROVAL_WEBAPP_ENABLED=True
ORDER_APPROVAL_WEBAPP_REQUIRE_TELEGRAM_AUTH=True
ORDER_APPROVAL_WEBAPP_AUTH_MAX_AGE_SECONDS=86400

# Processing
DEDUPLICATION_WINDOW_MINUTES=5
BATCH_PROCESSING_DELAY=1

# Multi-Tenant Configuration (optional - for multiple groups)
# If using multiple Telegram groups, configure GROUP_MAPPING as JSON
# Single group mode (MVP): Leave empty, uses GOOGLE_SHEET_ID
# Multi-group mode: Provide GROUP_MAPPING_JSON with chat_id -> sheet_id mapping
# Example (single group):
# GROUP_MAPPING_JSON={"100123456789": {"sheet_id": "1a2b3c...", "sheet_name": "Complaints Register"}}
# Example (two groups):
# GROUP_MAPPING_JSON={"100123456789": {"sheet_id": "1a2b3c...", "sheet_name": "Complaints Register"}, "100987654321": {"sheet_id": "xyz789...", "sheet_name": "Support Tickets"}}

# Default group ID for single-group deployments
DEFAULT_GROUP_ID=default

# API Rate Limiting (optional)
RATELIMIT_ENABLE=False
RATELIMIT_PER_IP=100/h
